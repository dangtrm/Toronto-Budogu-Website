import React from 'react';

const ProductListEdit = () => {
    return (
        <div>
            <h1>Now editing stock as admin</h1>

            <button id='round'>Add new item</button>
        </div>
    );
};

export default ProductListEdit;